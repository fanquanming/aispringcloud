spring:
  cloud:
    config:
      name: configclinet
      label: master
      discovery:
        enable: true
        service-id: configserver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/